# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
env:
  VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
  VITE_FIREBASE_AUTH_DOMAIN: "vectiq-erp-prod.firebaseapp.com"
  VITE_FIREBASE_PROJECT_ID: "vectiq-erp-prod"
  VITE_FIREBASE_STORAGE_BUCKET: "vectiq-timesheet.firebasestorage.app"
  VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID}}
  VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID}}
  VITE_FIREBASE_API_URL: "https://australia-southeast1-vectiq-erp-prod.cloudfunctions.net"
  VITE_APP_URL: "http://timesheet.vectiq.digital"
  VITE_EMAIL_SECRET: ${{ secrets.VITE_EMAIL_SECRET}},
  VITE_REGION: "australia-southeast1"
jobs:
  build_and_deploy:
    environment: Prod
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_VECTIQ_ERP_PROD }}
          channelId: live
          projectId: vectiq-erp-prod
          target: production
